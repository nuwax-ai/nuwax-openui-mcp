## Agent skills

### Issue tracker

Issues and PRDs are tracked in GitHub Issues for
`nuwax-ai/nuwax-openui-mcp`. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the canonical `needs-triage`, `needs-info`, `ready-for-agent`,
`ready-for-human`, and `wontfix` labels. See
`docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repository using root `CONTEXT.md` and `docs/adr/`.
The associated Host/consumer is https://github.com/nuwax-ai/nuwax — see
`docs/agents/domain.md`.
